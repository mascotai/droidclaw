meta {
  name: List Runs
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v2/devices/{{deviceId}}/workflows/runs
  body: none
  auth: bearer
}

params:query {
  limit: 5
  ~offset: 0
  ~status:
}

auth:bearer {
  token: {{apiKey}}
}

docs {
  List workflow runs for a device. Use status=running to see what's active. Supports limit, offset, and status filtering.
}
