{
  "name": "Instagram Ensure Account",
  "description": "Checks if the correct Instagram account is active. Tries to switch if not. Fails if the account can't be activated — the caller should handle login separately.",
  "params": {
    "username": "The Instagram username that should be active"
  },
  "steps": [
    {
      "id": "ensure_account",
      "app": "com.instagram.android",
      "goal": "Open Instagram. Dismiss any popups or dialogs. Check if the account '{{username}}' is currently logged in and active. Look at the profile tab or the username at the top of the screen. If '{{username}}' is the active account, you are done. If a different account is active, long-press the profile picture icon in the bottom-right corner to open the account switcher. If '{{username}}' appears in the list, tap it to switch. If '{{username}}' is not in the list, mark as failed.",
      "maxSteps": 12,
      "retries": 1,
      "eval": {
        "states": {
          "active_username": {
            "type": "string",
            "description": "The Instagram username currently visible at the top of the screen or in the profile tab (the active/logged-in account username)",
            "expected": "{{username}}"
          },
          "on_home_feed": {
            "type": "boolean",
            "description": "The Instagram home feed is visible with posts/stories and bottom navigation bar"
          }
        }
      }
    }
  ]
}
