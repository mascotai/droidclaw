FROM oven/bun:1

ARG GIT_SHA=unknown
ENV GIT_SHA=${GIT_SHA}

WORKDIR /app

COPY package.json ./
COPY packages/shared ./packages/shared
COPY server ./server

RUN bun install

WORKDIR /app/server
EXPOSE 8080
CMD ["bun", "src/index.ts"]
